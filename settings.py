nullMovePruning = False # overall, incorrect. some error implementing it
moveOrdering = False # is beneficial, but takes too long to sort rather than the nodes it prunes
quiesce = False # True is fine here, might have some lower depths though, which cause it make bad decisions
